/**
 * Header — Now only renders the TickerBar (activity feed + sponsors).
 * All navigation has moved to BottomNav.
 */
import TickerBar from './TickerBar';

export function Header() {
  return <TickerBar />;
}
